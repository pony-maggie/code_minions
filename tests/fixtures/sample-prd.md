# PRD — Calculator

## Goal
Add an `add(a, b)` function to `calc` package.

## Users
Library consumers.

## Features
- FR-1 add: function `calc.add(a, b)` returns sum. Acceptance: `add(2, 3) == 5`.

## Non-functional
- No external deps.
